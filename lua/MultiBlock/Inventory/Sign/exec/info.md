# <code>es.MultiBlock.Sign.Text</code> executables

arbitrary texts in `es.MultiBlock.Sign.Text` are supposed to be stored as is.

however, some texts may execute internal scripts.

such behaviors must be moved to it's own items, to improve usability.
