when using `es.CreateMultiblock()` , avoid creating blocks outside the limits.

according to moderation rules in the official **Evertech Sandbox Mods (VK)** community, it may be treated as **shoplifting** or **AD-restriction bypassing**.

only create such blocks if necessary for enum validation or testing purposes.

`Cube`
`Wheel`